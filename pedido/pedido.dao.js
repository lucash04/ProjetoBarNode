const Pedido = require('./pedido.model');
const db = require('../db/db');

module.exports = {

    consultar : (callback) => {

        // consultar todos os pedidos

    },

    registrar : (pedido, callback) => {

        // registrar os pedidos, com as informações que viram da 'tela'

    }

}